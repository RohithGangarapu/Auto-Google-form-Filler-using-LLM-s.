# Frontend

Vite React frontend for the LLM Form Automation Assistant.

## Run

```bash
npm install
npm run dev
```

By default the app calls `http://localhost:8000/api`. Override with `VITE_API_BASE_URL` when needed.
